# Agent Directives

- **Check Memory First:** Before starting any file indexing or architectural plan, search the `memory` server for existing project context.
- **Auto-Persist:** When key decisions, library setups, or bug fixes are confirmed, save them using `create_entities` or `add_observations`.
- **Token Optimization:** Keep responses concise and refer to existing knowledge graph entities instead of re-parsing source code.
